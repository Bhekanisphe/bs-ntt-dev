URL:
    https://bucket.s3.aws-region.amazonaws.com/key
    https://s3.aws-region.amazonaws.bucket/key
    key = object name

    maximum = 6TB

    S3 gateway Endpoint - Access s3 using private IP

    Durability:
        Durabilitiy is protection against:
            Data lost
            Data corruption
            S3 offers 11 9s durability
    Availability:
        Availability is a measurement of:
            The amount of time the data is available to you
            Expressed as a percent of time per year
Policies:   
    Bucket policies are resource based policies
    Can be attached to S3 buckets
    Also a JSON

ACL
    Legacy access control mechanism that predates IAM
    AWS generally recommends using Bucket polices
    Use cases:
Principlal = who

Deny always overwrites allow

Versioning:
    Keeps multiple varients of an object in the same bucket
    Preserve, retrieve and restore every version of every object
    Recover from *accidential deletion or overwrites*

replication:
    CRR and SRR - Must have versioning enables
    Cross region and same region

MFA:
    Required for: Changing the versioning state of a bucket 
    Permanently deleting an object version

    the x-amz-mfa request header must be included
    versioning must be enabled

    MFA delete can be enabled by the *bucket owner*(root acc)

    MFA-protecting Access
        Used to enforce auth factor when accessing AWS resource
        Enforced using aws:MultiFactorAutAge key in a policy bucket

Encryption:
    Server-side encryption with S3 managed key (SSE-S3) (Default)
        s3 managed keys
        Unique object keys
        master key
        AES 256
    Server-side encryption with AWS KMS managed keys (SSE-KMS)
        KMS manages keys
        can be aws managed keys
        can be customer managed keys
    Server-side encryption with client provided keys (SSE-C)
        client managed keys
        Not stored on AWS
    Client side encryptions
        Client managed keys
        not stored in aws
        you can use KMS keys

    can also encrypt objects by using the *CopyObject* API or *copy-object* CLI command

aws s3 presign <s3link> -- valid for an hour

Server Access Logging
    Provides detailed records for the requests that are made to a bucket
    Details inclue: the requester, buckrt name, request time, request action, response status, error code
    Configure separate bucket as destination
    Must grant permission to S3 log delivery group on destination bucket

CORS
    Access-Control-Allow-Origion
    Access-Control-Allow-Methods
    Access-Control-Allow-Headers

    Defined though rules, added to JSON

CloudFront
    Origions (Where your files are)
        Content is pushed from the origion and cached
        Edge locations are destributed around the world
        Users are directed to the closest edge location
    Distribution
        Web distribuuution:
            Speed up distribution of static and dynamic conten for example, html, css php and graphic files
            Can be used to stream media files using RTMP
            distribute media files using HTTP/S
            Add, Update or delete object and submit data from web forms
            Use live streaming to stream an event in real time

    Signed URLs
        Provide more control over access to content
        Can specify begining and exiation  date/time, IP addresses
    Signed Cookies
        Use signed cookies when you don't want to change URLs
        Can also be used when you want to provide access to *multiple restricted files* (Suigned URLs are for inddividual diles)
    Origin Access Identity
        Use bucket policy to access
    Origion Access Control
        Requires an bucket policy that allows the CloudFront service principal

    Route53 (DNS Service)
        Custom domain name
        Do heallth checks
        Route you to the closest region within a geographic area

Transfer accelaration speeds up data uploads using CloudFront network




